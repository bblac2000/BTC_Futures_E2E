# 시행 레지스트리 (append-only)

> 규칙: 삭제·수정 금지, 추가만. 틀린 행은 고치지 않고 정정 행을 덧붙인다(행 번호 인용).
> 파라미터·임계 변경 = 새 행 + 새 사전등록. 실패 후보 이름 바꿔 재제안 금지(research-protocol §4·§7).
> 판정 3분류 ACCEPT / REJECT / 폐기 · 기각은 검정력 부족 vs 효과 부재를 구분한다.

| # | 일자(UTC) | 트랙 | 대상 | 파라미터(해시) | 게이트·OOS 앵커·N | 상태 | 승인·검토자·근거 |
|---|---|---|---|---|---|---|---|
| 1 | 2026-09-15 | 운영·데이터 전달 감시 (layer 5 가동 전 사전확약) | `ops/delivery_counter.DELIVERY` — BTCUSDT `/market` 소켓 스트림 전달 임계 | **kline1m_update** `@kline_1m` 모든 push(x=false·x=true)·ts=`E` · min_per_min=**1** · grace_sec=**120** / **kline1m_close** `@kline_1m` x=true만·ts=`E` · min_per_min=**0**(분당 검사 없음) · grace_sec=**120**(마지막 마감 이후 나이 = 마감 2회) / **markprice** `@markPrice@1s` · min_per_min=**1** · grace_sec=**120**. 판정 규칙 `is_stalled()`(E2E #134 동일): 한 번도 안 옴 → 시작 후 grace 경과 시 정지 · 마지막 수신이 grace 초과 → 정지 · min_per_min>0이면 직전 벽시계 완결 분 행 수 미달 → 정지(관측 없던 분 = 0) | 게이트 아님(판정·성과 집계에 들어가지 않는다) · 소비자 = layer 5 live 카운터 → layer 7 stale-data kill 입력, layer 8 health 재생 | 🔒 **사전확약(PRE-COMMIT)** — 이 봇의 스트림 데이터를 **한 건도 보기 전**에 커밋. 🚫 결과를 본 뒤 숫자 변경 금지 — 바꾸려면 새 행 | ✅ **사용자 확정 2026-09-15**(옵션 3 "감시 둘"). 근거: ① **update** = 스트림 침묵 감지. 형성 중 push ~250ms(v6 §10)라 분당 1건은 느슨한 하한이고 120초는 "2분 무수신". ② **close** = "업데이트는 흐르는데 봉이 안 닫힘" 감지. 마감은 분당 정확히 1회라 min_per_min=1이면 **주기와 딱 맞는 분 검사**가 되고, 마감 `E`가 봉 종료 `T`(xx:59.999)와 같게 찍히면 앞 분에 들어가 다음 벽시계 분이 0건 → **가짜 정지**. 그래서 분당 검사를 끄고 나이 규칙만 둔다(정상 간격 ~60초 < 120초 = 마감 2회) — 테스트 `test_close_monitor_ignores_the_59_999_boundary_jitter_that_a_minute_rule_would_flag`. ③ **markprice** = 2026-09-04 로컬 600초 실측 601틱·간격 1.000초·결손 0(메모리 기록), E2E #134와 같은 형태. 🔒 테스트 `test_thresholds_are_exactly_the_pre_committed_values`가 값을 잠근다. 검토자: 사용자(결정)·Claude Code(구현) |
| 2 | 2026-09-15 | 설계 정정 · 사이징 규칙 (layer 2) | **스킬 `strategy-modules.md §1` 사이징 식 대체** + v6 §1.0·§5.4 "liquidationFee는 청산가 공식의 MMR에 가산" 문장 정정 | **B1 청산 거리 = 거래소 공식**(v6 §4.4): `liq_dist(L) = 1/L − MMR_eff`, `MMR_eff = MMR_bracket − cumB/notional`. **liquidationFee는 거리에 없다** — 청산 뒤 남은 격리 마진에서 부과 → "청산 = 격리 마진 전액 손실(+ 명목×fee)"로만 모델링(`loss_at_liquidation_usdt`). 게이트 `sl_dist × buffer < liq_dist(L)`. 진입 후 진리원 = `positionRisk.liquidationPrice`, 공식은 진입 전 추정. 진입 후 검사: `거래소 청산 거리 × buffer < 추정 청산 거리` → **CHECK**(로그·점검) — "버퍼 이상 가깝다"의 해석. **B2 위험 예산이 1차 제약**: ① budget = equity × risk_pct(regime) ② notional_target = budget / sl_dist ③ L = [l_min, l_max] 안 최고 정수 중 **목표 명목의 브라켓** 상한·게이트 통과, 없으면 거부 ④ margin = notional/L, pos_pct = margin/equity(**도출**) ⑤ **pos_pct_max 0.40 하드 캡**(초과 시 명목 = 0.40×equity×L, 손실은 예산 아래 — 허용) · **pos_pct_min 0.10 권고만**(기록, 부풀리지 않음) ⑥ 최종 qty 내림 후 `loss_at_sl > budget × (1 + loss_tolerance)`면 거부(tolerance 기본 0). 레짐은 (risk_pct, l_min, l_max)만 준다 | 게이트 아님(사이징 규칙) · 영향 = layer 2 `sizing/` 전면 교체(구 식 코드 `d1ed96d`·`327582f`는 대체됨) | ✅ **확정** — 구 식은 **오류로 대체**(스킬 §1의 `− liquidationFee` 항, `L_raw = risk_pct/sl_dist` + `notional = equity×pos_pct×L`). 🚫 되돌리려면 새 행 | ✅ **사용자 결정 2026-09-15** ("B1·B2는 스킬 식의 오류"). 발견 경위: layer 2 1차 구현(구 식 그대로)에서 fixture 값으로 100x가 어떤 SL로도 불가능·클램프 시 risk_pct 무의미를 Claude Code가 보고 → 사용자 판정. 근거 문서: v6 §4.3(`MM = notional×MMR − cum_b`)·§4.4, 사본 v6 §1.0·§5.4 정정 부기(2026-09-15). ⚠️ 스킬 파일 자체는 이 저장소에서 수정하지 않았다(사용자 정본) — 이 행이 대체 기록이다. 검토자: 사용자(결정)·Claude Code(구현)·Codex(재검토 예정) |
| 3 | 2026-09-15 | 사이징 파라미터 사전확약 (layer 3 페이퍼 가동 전) | `sizing.config.SizingLimits.buffer` — SL 거리 대비 청산 거리 안전 배수(q_c 계열) | **정의만 확정, 값 미정(PENDING)**: 게이트 `sl_dist × buffer < liq_dist(L)`에 쓰이고 진입 후 CHECK 판정(`exchange_dist × buffer < estimate_dist`)에도 같은 값이 쓰인다. 코드 제약 = `buffer ≥ 1`(미만은 게이트를 느슨하게 함 → 예외), **기본값 없음**(필수 인자 — 조용한 기본값이 게이트를 정하지 않게) | 게이트 파라미터 · 값 확정 전에는 페이퍼 가동 불가(layer 3 기동 시 설정 누락 = 기동 실패) | ⏸ **값 사전등록 대기** — 스킬 research-protocol §5 "합성 SL-교차법으로 버퍼 보정(q_c=0.9995 @1m)"이 산출 방법 후보. 🚫 결과를 본 뒤 값 선택 금지 | ✅ 사용자 결정 2026-09-15 ("buffer는 config 값으로 두고 자체 레지스트리 행"). 값·산출 방법은 사용자 결정 대기 |
| 4 | 2026-09-15 | 설계 정정 · 청산식 (layer 2) — **#2의 청산 거리 부분을 대체**(append-only) | `sizing.position.liquidation_estimate` · 사이징 게이트 · 진입 후 검사 | **B5 (a) 방향별 정확식** — 바이낸스 FAQ "How to Calculate Liquidation Price of USDⓈ-M Futures Contracts"(2025-12-31 갱신) 원식 `LP = (WB − TMM + UPNL + cum_B − Side·Pos·EP) / (Pos·MMR_B − Side·Pos)`에 격리·원웨이(TMM=0·UPNL=0)·신규 포지션을 대입: **LONG `dist = (1/L − taker − MMR_eff)/(1 − MMR)`** · **SHORT `dist = (1/L − taker − MMR_eff)/(1 + MMR)`**, `MMR_eff = MMR − cum/N`. **티어 = max(진입 명목, 청산가 명목)의 브라켓**(청산가 명목이 다른 티어면 그 티어로 재계산 — SHORT에서 의미). **진입 수수료 가정(보수)**: taker 수수료가 격리 마진을 줄인다 `WB = N/L − N×taker`(taker = 런타임 commissionRate). 실제 여부는 진입 후 `positionRisk.liquidationPrice`를 수수료 차감/미차감 두 모델과 비교해 **차이와 더 가까운 모델을 로그** → 첫 페이퍼 주에 판정 | 게이트 식 · #2의 "`1/L − MMR_eff`" 문구는 **이 행으로 대체**(SHORT에서 반보수였음) | ✅ **확정** · 🚫 결과를 본 뒤 식 변경 금지 — 수수료 가정 판정 결과로 바꾸려면 새 행 | ✅ **사용자 결정 2026-09-15**(B5 (a)). 근거: Codex 재검토(`task-mu27w8m4-oicyfq`) Q2 → Claude Code가 공식 FAQ 식 이미지를 받아 판독·수치 대입으로 확인(ops_log) → 사용자 유도 검증. 반례(SHORT 60,000·SL 60,359.4)는 100x 불가로 테스트 고정. 구현 `0c78d37` 이후 커밋. 검토자: 사용자(결정)·Codex(Q2 제기, 재검토 예정)·Claude Code(구현) |
| 5 | 2026-09-15 | 사이징 파라미터 사전확약 — **#3(값 PENDING)을 확정**(append-only) | `sizing.config` `SL_BUFFER_REL`·`SL_LIQ_MIN_GAP`·`SL_TRIGGER_BASIS` | **두 조건 모두 필수**: ① `sl_dist × b_rel < liq_dist`, **b_rel = 1.5** ② `liq_dist − sl_dist ≥ 10 bps`(0.0010, 절대). **SL 트리거는 mark 가격 기준**(청산과 같은 기준 · last 아님). 진입 후 CHECK 판정도 `exchange_dist × 1.5 < estimate_dist`. **근거**: MARKET 청산 슬리피지 · SL 평가와 청산 평가 사이의 mark 이동 · 모니터 지연을 덮는다. 📌 fixture 값에서 BTC 50~100x 청산 거리 ≥ 0.55%라 ② 10bp 하한은 ①보다 느슨해 **현재 레버리지 범위에서는 결박되지 않는다**(기록) | 게이트 파라미터 | 🔒 **사전확약 — 이 봇의 시장 데이터를 보기 전에 커밋**. 📅 **재평가: 페이퍼 14일차**에 우리 체결 기준(슬리피지 p99, 모니터 주기의 mark 이동 p99)으로 **새 행**에서만. 🚫 사후 완화 금지 | ✅ **사용자 결정 2026-09-15**. 선택적 사전 측정(사전등록 불요 데이터 소스 측정): E2E bookTicker + markprice 1s의 `|mark − mid|/mid` p99 — 결과는 ops_log(측정일 기준) |
| 6 | 2026-09-15 | 정정 · **#4의 "수수료 가정 판정 시점" 문구만 대체**(append-only · 식·가정 자체는 불변) | #4 진입 수수료 가정(`WB = N/L − N×taker`)의 **검증 경로** | #4는 "진입 후 `positionRisk.liquidationPrice`로 첫 **페이퍼** 주에 판정"이라 적었으나 **페이퍼 포지션은 거래소에 존재하지 않아** `positionRisk`에 비교 대상이 없다. 페이퍼에서 계정을 조회하면 사용자가 수동으로 든 **다른 포지션**과 비교하게 된다(잘못된 대상 — 데이터처럼 보이는 오류). 따라서: ① `post_entry_liquidation_check`는 **`Mode.LIVE` + 이 봇이 낸 포지션**에서만 호출 ② PAPER는 `liquidation_estimate`(#4 모델)로 청산을 시뮬레이션하고 `liq_price_est`만 기록 — `positionRisk` 미사용 ③ 판정은 **첫 라이브 진입부터** 로그(fee/no_fee gap · closer_model 틱 기준). `no_fee`가 일관되게 더 가까워도 **게이트를 느슨하게 하려면 새 행** | 게이트 아님(검증 경로) · 라이브 체크리스트 항목 문구 갱신 | ✅ 확정(문구 정정) · 🚫 #4 식·가정 불변 | 발견: Claude Code(layer 3 설계 전 advisor 검토). ⚠️ **미결 — 사용자 결정 필요**: 라이브 전에 테스트넷 또는 최소 명목 실계정 프로브로 판정을 앞당길지(자금·권한 결정이라 Claude Code가 고르지 않는다). 검토자: 사용자(결정 대기)·Claude Code(기록) |
| 7 | 2026-09-15 | 페이퍼 체결 모델 사전확약 — **스킬 exchange-rules §6의 0.016 bps를 페이퍼에 한해 대체**(append-only) | `paper.config.PAPER_SLIPPAGE_RATE` | **2 bps(0.0002) 편도**, 불리한 방향 + **불리한 tick 반올림**(BUY 올림·SELL 내림). 체결 기준가 = mark. 수수료는 여전히 런타임 taker. **근거**: 이 저장소의 7일 측정(2026-08-08~14, 604,425초, ops_log 2026-09-15) `|mark − mid|/mid` **p99 1.835 bps**(p99.9 3.411 · 최대 31.8) — mark로 체결하는 모델이 호가 체결과 벌어지는 크기의 p99를 편도마다 적용. 0.016 bps(E2E Phase 1-A 왕복 실측)는 mark가 아니라 호가 대비 실측이라 mark 기준 모델에는 방어가 안 된다(Codex L3 검토 Q6(f)) | 게이트 아님(비용 모델) · 페이퍼 손익·사이징 예상 체결가(`quote_fill_price`)에 들어간다 | 🔒 **사전확약 — 페이퍼 시작 전**. 📅 **재평가: 페이퍼 14일차**, 우리 체결(라이브 전이면 페이퍼 신호 시점 호가 대비 가상 체결 괴리) 기준 **새 행**에서만. 🚫 결과를 본 뒤 완화 금지 | ✅ **사용자 결정 2026-09-15**(결정 3). 검토자: 사용자(결정)·Codex(Q6(f) 제기)·Claude Code(구현) |
| 8 | 2026-09-15 | 거래소 규칙 표면 — **전송 라이브러리 고정**(append-only) | `pyproject.toml` · `uv.lock` | **`ccxt==4.5.78`**(ccxt.pro 포함, REST `binanceusdm` · WS `ccxt.pro.binanceusdm`) · WS 전송 `aiohttp==3.14.3`(lock). ccxt는 **전송으로만** 쓴다: exchangeInfo 필터·브라켓(cum)·commissionRate·fundingInfo·positionRisk는 **원시 `info`/원시 엔드포인트 응답**에서 읽고 ccxt 정규화 필드를 쓰지 않는다 · 수량 정규화(floor → MIN_NOTIONAL 재확인 → tick)는 우리 코드(`amount_to_precision`을 최종 검사로 쓰지 않는다). WS 티어는 `tests/test_ccxt_stream_tiers.py`가 **ccxt.pro가 실제로 여는 URL과 SUBSCRIBE 스트림**을 `ops/stream_tiers.py` 대응표로 검증(legacy `/ws`·`/stream` 또는 티어 불일치 → 실패) | 게이트 아님(전송 계층) — 단 티어 테스트는 CI 필수 | 🔒 **정확 고정**. 업그레이드 = **새 행** + 티어 테스트·watch 메서드 목록 재검증 + 원시 응답 모양 재확인. 🚫 범위 지정(`>=`) 금지 | ✅ **사용자 결정 2026-09-15**(테스트넷 폐기 · ccxt/ccxt.pro 채택). 검토자: 사용자(결정)·Claude Code(구현)·Codex(검토 예정) |
| 9 | 2026-09-15 | 부기 · **#7의 적용 범위 확장**(append-only · 값 불변) | `paper.sender.adverse_fill_estimate` · `PaperSender.quote_fill_price` · `LiveSender.quote_fill_price` | #7 모델(편도 **2 bps 불리** + **불리 tick**, `PAPER_SLIPPAGE_RATE` 0.0002)이 **LIVE 사이징의 예상 체결가에도** 적용된다. 한 함수 `adverse_fill_estimate`를 PAPER·LIVE가 같이 부른다 — 모드별로 다른 건 **실제 체결**뿐. 이전 LIVE 추정은 mark 그대로였다(모든 진입이 #5 경계에 붙어 실제 슬리피지가 곧 체결 후 청산). 실제 LIVE 체결이 추정보다 나쁘면 체결 후 #5 재검증이 즉시 청산한다(변경 없음). 테스트: `test_live_fill_exactly_at_the_registry_7_estimate_passes_the_post_fill_gate`(LONG·SHORT, 실제 `LiveSender` 경로) · `test_live_quote_uses_the_registry_7_adverse_model_same_as_paper` | 게이트 아님(사이징 입력 가격) | 🔒 #7과 같이 페이퍼 14일차 재평가(새 행) · LIVE 체결 괴리가 생기면 그 데이터로 새 행. 🚫 사후 완화 금지 | ✅ **사용자 결정 2026-09-15**("Live fill estimate uses the registry #7 model … Same code path for paper and live"). 검토자: 사용자(결정)·Claude Code(구현)·Codex(검토 예정 — LiveSender 변경) |
| 10 | 2026-09-15 | 안전 게이트 파라미터 사전확약 (layer 7 · 페이퍼 가동 전) | `safety.config.KillSwitchLimits` — `daily_loss_pct`(x) · `max_consecutive_losses`(n) | **정의만 확정, 값 미정(PENDING)**: 킬스위치(strategy-modules §6) = ① 일일 손실: UTC 날짜 첫 equity 관측 대비 `equity ≤ 기준 × (1 − x)`(경계 포함) ② 연속 손실: 청산마다 거래 손익 = 청산 뒤 지갑 − 직전 flat 지갑(진입·청산 수수료·펀딩 포함) < 0 이 n회 연속 ③ 청산 1회(스킬 고정, `MAX_LIQUIDATIONS=1`). 발동 → 신규 진입 중단 + 알림 · 해제는 사람의 /start만 · 상태는 DB `safety_state`에 남아 재시작이 풀지 않는다. 코드 제약 = 0 < x < 1(Decimal) · n ≥ 1 · **기본값 없음**(필수 인자) | 게이트 파라미터 · 값 확정 전에는 기동 불가(layer 8 설정 누락 = 기동 실패) | ⏸ **값 사전등록 대기** — 🚫 페이퍼 결과를 본 뒤 값 선택 금지 · 테스트의 0.05·3은 테스트 픽스처일 뿐 제안값이 아니다 | 정의: Claude Code(layer 7 구현, 스킬 §6 문언). 값: **사용자 결정 대기**. 관련: rate-limit 80%는 스킬 §6 값 그대로(`RATE_LIMIT_RELAX_AT`) · stale-data 임계는 레지스트리 #1 재사용(새 숫자 없음) |
| 11 | 2026-09-16 | 안전 게이트 파라미터 사전확약 — **#10(값 PENDING)을 확정**(append-only) | `safety.config.REGISTERED_KILL_SWITCH` · `DAILY_LOSS_PCT` · `MAX_CONSECUTIVE_LOSSES` · `MAX_LIQUIDATIONS` · `MAX_VANISHED` | 킬스위치 발동 조건(하나라도 → 신규 진입 중단 + 알림 · 해제는 사람의 /start만): ① **일일 손실 5%** — UTC 날짜 첫 equity(지갑 + mark 기준 미실현) 대비 `equity ≤ 기준 × 0.95`(경계 포함) ② **연속 순손실 5회** — 청산 뒤 지갑 − 직전 flat 지갑 < 0(진입·청산 수수료·펀딩 포함 · 부분 청산은 거래가 아님) ③ **청산 1회** ④ **포지션 소실 1회**(LIVE `PositionVanished` — 청산과 별도 계수). 정의는 #10 그대로 | 게이트 파라미터 | 🔒 **사전확약 — 페이퍼 가동 전(이 봇의 거래 결과 0건)**. 📅 **재평가: 페이퍼 14일차, 새 행으로만**. 🚫 결과를 본 뒤 완화 금지 | ✅ **사용자 결정 2026-09-16**("#10 kill switch: daily loss 5% of start-of-UTC-day equity; 5 consecutive net-losing trades; 1 liquidation; 1 vanished position"). 테스트 `test_kill_switch_values_are_exactly_registry_11`이 값을 잠근다. 검토자: 사용자(결정)·Claude Code(구현)·Codex(layer 8 배치 검토 예정) |
| 12 | 2026-09-16 | 안전 규칙 사전확약 — **stale 데이터 중 포지션 자동 청산**(append-only · 새 숫자 없음) | `safety.stale.StaleDataGuard` · layer 8 안전 틱 · `ExitReason.STALE_DATA` | ① 진입 차단 = 레지스트리 #1 `stalled()` 그대로(분당 규칙 포함 · 정지가 풀릴 때까지). ② **포지션이 있고 어느 #1 스트림이든(kline1m_update·kline1m_close·markprice) 마지막 수신 나이 > 그 스트림의 `grace_sec`(120초)** — 한 번도 안 왔으면 관측 시작 후 120초 초과 — 이면 **MARKET reduceOnly 전량 청산**(사유 `stale_data`) · 실패하면 정지가 계속되는 동안 안전 틱마다 재시도. #1의 분당 규칙으로만 정지(나이 ≤ 120초)면 청산하지 않고 진입만 막는다(`hold_and_alert`). ③ **모든 stale 청산을 알린다**. ④ PAPER stale 청산의 체결 기준가 = **마지막으로 받은 mark**(더 새 값이 없다) — `orders.ref_mark`·`positions.reason='stale_data'`로 꼬리표가 남아 14일차 분석에서 분리 가능 | 안전 규칙(성과 판정 아님) | 🔒 **사전확약 — 페이퍼 가동 전**. 🚫 결과를 본 뒤 변경 금지 — 바꾸려면 새 행 | ✅ **사용자 결정 2026-09-16**. 근거(사용자 원문): "at 50–100x the liquidation distance is ~0.6% and the SL cannot be monitored without data; a false-stale close costs ~5 bps, a real outage costs the whole isolated margin. Alert on every such close." 해석 기록(Claude Code): "stale data"를 #1의 세 스트림 전부로 읽었다(markprice만이 아니라) — 보수 쪽. 검토자: 사용자(결정)·Claude Code(구현)·Codex(검토 예정) |
| 13 | 2026-09-16 | 운영 통제 확정 — 텔레그램 명령 의미 · 대사 해제 · 일일 손실 날의 /start(append-only) | `notify.bot` · `safety.gate.SafetyGate.resume` · `safety.reconcile.ReconcileGuard` · layer 8 `BotController` | ① **/stop** = 신규 진입 차단 + 전량 청산(확인 흐름) · **/close** = 전량 청산만(진입 상태 불변 · 확인 흐름) · **/pause** = 신규 진입 차단, 포지션 유지(확인 없음). ② 대사 **수량·방향 불일치는 사람만 해제**(sticky) · 거래소 조회 일시 실패(`exchange_unavailable`)는 다음 성공 조회로 자동 해제. ③ **일일 손실로 킬스위치가 발동한 UTC 날짜에는 /start가 아무것도 바꾸지 않는다**(일시정지·대사·엔진 차단도 그대로) — 응답에 글자 그대로 "blocked by daily-loss limit until 00:00 UTC". 00:00 UTC 이후에도 자동 해제 없음(사람의 /start 필요) | 운영 규칙 | ✅ 확정 · 🚫 바꾸려면 새 행 | ✅ **사용자 결정 2026-09-16**("Confirmed per 기본환경설정"). 검토자: 사용자(결정)·Claude Code(구현) |
| 14 | 2026-09-16 | 안전 규칙 정정 — **#12 ②(청산 조건)를 대체**(append-only · 새 숫자 없음 · #12 ①③④ 유지) | `safety.stale.CLOSE_STREAMS` · `StaleDataGuard.update` · layer 8 안전 틱 | ① 포지션 자동 청산(MARKET reduceOnly · 사유 `stale_data`)은 **markprice 스트림만** 본다: 마지막 수신 나이 > `grace_sec`(120초, 레지스트리 #1) — 한 번도 안 왔으면 관측 시작 후 120초 초과. ② **kline1m_update·kline1m_close**가 grace를 넘겨도 **청산하지 않는다** — 진입 금지 + 보유 + 알림(`hold_and_alert`). ③ 어느 스트림이든 #1 **분당 규칙** 위반 → 진입 금지만. ④ 청산 실패 재시도·모든 stale 청산 알림·PAPER 체결 기준가(마지막 mark)는 #12 그대로 | 안전 규칙(성과 판정 아님) | 🔒 **사전확약 — 페이퍼 가동 전(이 봇의 거래 결과 0건)**. 🚫 바꾸려면 새 행 | ✅ **사용자 결정 2026-09-16**(#12 해석 정정). 근거(사용자 원문): "the SL is evaluated on mark, so mark stall = SL cannot be monitored. Kline update/close stalls block entries but do not close the position. Any per-minute-rule breach → block entries only." 연결: #5 `SL_TRIGGER_BASIS` = mark. #12의 해석 기록("세 스트림 전부")은 이 행으로 대체. 테스트 `test_registry_14_only_a_markprice_stall_past_grace_closes_kline_stalls_hold` · `test_registry_14_kline_streams_past_grace_block_entries_but_keep_the_position`. 검토자: 사용자(결정)·Claude Code(구현)·Codex(layer 8 후속 배치 검토 예정) |
| 15 | 2026-09-16 | 운영 통제 확정 — 재기동 복원·규칙 출처의 조건부 승인 + 직전 실행 dirty 판정(append-only · 새 게이트 숫자 없음) | `ops.restore` · `ops.run_bot.load_rules` · `ops.run_events` · `ops.health` · `paper.sender.LiveChecklist` | ① 재기동 불일치로 고아 포지션을 `restart_unrestored`(손익 없음 · 킬스위치 미계수)로 닫는 것 **승인** — 조건: **확정 사실 알림을 하루 한 번**, 사람이 **/start로 확인할 때까지 /status에 표시**(`SafetyGate.notices` · safety_state 저장). ② 키 권한 조회의 **전송 오류** → 스냅샷 기동 + `rules_from_snapshot` 차단 **승인** — **거래 권한이 확인된 키만** 종료 코드 4. ③ 런타임 규칙 **기동 때만 조회는 페이퍼에 한해 수용** — LIVE는 **주기 재조회(≥ 6시간마다) + 24시간 넘은 규칙으로는 진입 금지**가 필수 체크리스트 항목(`LiveChecklist.runtime_rules_refresh_6h_and_entries_need_rules_under_24h`). ④ 일시정지 사유 단일값은 **페이퍼에 한해 수용** — LIVE 전 집합으로(`LiveChecklist.pause_reasons_are_a_set`). ⑤ 기동 때 봇 대장에 **stop 없는 start/connect**가 있으면 `dirty_previous_run`으로 기록하고 **확정 사실 알림 한 번** — SIGKILL이 dirty-stop 검사를 공짜로 통과하지 않게. ⑥ SIGKILL 때 봇 raw shard의 미기록 버퍼(드라이런 ~17초) 유실은 **수용** — E2E 수집기가 정본 | 운영 규칙 · LIVE 체크리스트 | ✅ 확정 · 🚫 바꾸려면 새 행 | ✅ **사용자 결정 2026-09-16**("Choices approved with conditions" 1–4 · "One small addition before deployment"). 테스트: `test_restart_unrestored_stays_in_status_until_start_and_survives_a_restart` · `test_confirmed_facts_in_the_status_file_alert_daily_or_once` · `test_a_previous_run_with_start_or_connect_but_no_stop_is_recorded_as_dirty_exactly_once` · `test_live_sender_requires_every_checklist_item`. 검토자: 사용자(결정)·Claude Code(구현)·Codex(배포 전 배치 검토 예정) |
| 16 | 2026-09-16 | 배포 계획 확정 + 알림 보완(append-only) | `docs/runbook_vps.md` §9 · `ops/run_bot.py` 기동 알림 · prune 타이머 | ① 배포 두 날: **D1 = 2026-09-18**(사용자·코드·환경·VPS 드라이런 · 유닛 없음) · **D2 = 2026-09-19**(유닛 설치·기동) · 각각 **00:35–02:30 UTC**, 그날 E2E 00:10 판정 뒤 · 라이브 호스트 변경 하루 1건. 선행: Codex 배포 전 배치 MERGE + E2E Restart B 게이트 종료(**09-18 00:16 UTC**). 키 IP 화이트리스트는 D1 로컬 점검 뒤 마지막. ② prune 타이머는 미설치 유지 · D2 뒤 VPS `systemctl --user list-timers` 실측으로 **설치된 모든 E2E 타이머에서 ≥ 30분** 떨어진 시각을 별도 변경으로. ③ `dirty_previous_run`: health 확정 사실 알림 한 번 **유지** + 기동 알림에 **한 줄 언급** 추가 | 운영 계획 | ✅ 확정 · 🚫 바꾸려면 새 행 | ✅ **사용자 결정 2026-09-16**("Approved: 1. Two-day deploy … 2. Prune timer stays uninstalled … 3. keep the single health alert AND add the one-line mention"). 검토자: 사용자(결정)·Claude Code(준비)·Codex(배포 전 배치) |
